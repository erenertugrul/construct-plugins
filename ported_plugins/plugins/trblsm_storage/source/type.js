"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.trblsm_storage;

	PLUGIN_CLASS.Type = class trblsm_storageType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
