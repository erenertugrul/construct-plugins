"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_CSV2Array;

	PLUGIN_CLASS.Type = class Rex_CSV2ArrayType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
