"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_UserID2ID;

	PLUGIN_CLASS.Type = class Rex_Firebase_UserID2IDType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
