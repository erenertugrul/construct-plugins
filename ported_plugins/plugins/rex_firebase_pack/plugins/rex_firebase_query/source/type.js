"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_Query;

	PLUGIN_CLASS.Type = class Rex_Firebase_QueryType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
