"use strict";

{
	C3.Plugins.Rex_FirebaseAPIV3.Acts =
	{
		initializeApp(apiKey, authDomain, databaseURL, storageBucket)
		{
	        this.initializeApp(apiKey, authDomain, databaseURL, storageBucket);
		}
	};
}