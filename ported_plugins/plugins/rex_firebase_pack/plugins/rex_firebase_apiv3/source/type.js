"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_FirebaseAPIV3;

	PLUGIN_CLASS.Type = class Rex_FirebaseAPIV3Type extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
