"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_Leaderboard;

	PLUGIN_CLASS.Type = class Rex_Firebase_LeaderboardType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
