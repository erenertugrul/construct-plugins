"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_SyncQueue;

	PLUGIN_CLASS.Type = class Rex_Firebase_SyncQueueType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
