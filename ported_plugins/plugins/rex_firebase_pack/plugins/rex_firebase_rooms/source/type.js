"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_Rooms;

	PLUGIN_CLASS.Type = class Rex_Firebase_RoomsType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
