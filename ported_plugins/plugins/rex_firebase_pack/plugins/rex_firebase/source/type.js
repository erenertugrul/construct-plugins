"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase;

	PLUGIN_CLASS.Type = class Rex_FirebaseType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
