"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_Storage;

	PLUGIN_CLASS.Type = class Rex_Firebase_StorageType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
