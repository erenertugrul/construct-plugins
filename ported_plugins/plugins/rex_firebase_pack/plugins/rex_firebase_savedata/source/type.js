"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_SaveSlot;

	PLUGIN_CLASS.Type = class Rex_Firebase_SaveSlotType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
