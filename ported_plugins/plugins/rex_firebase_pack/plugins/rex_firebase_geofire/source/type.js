"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_Geofire;

	PLUGIN_CLASS.Type = class Rex_Firebase_GeofireType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
