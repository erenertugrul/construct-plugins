"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_SimpleMessage;

	PLUGIN_CLASS.Type = class Rex_Firebase_SimpleMessageType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
