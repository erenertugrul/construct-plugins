"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_Token;

	PLUGIN_CLASS.Type = class Rex_Firebase_TokenType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
