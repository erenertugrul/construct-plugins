"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_message;

	PLUGIN_CLASS.Type = class Rex_Firebase_messageType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
