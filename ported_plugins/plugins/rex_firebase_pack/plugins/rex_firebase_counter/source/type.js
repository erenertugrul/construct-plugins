"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_Counter;

	PLUGIN_CLASS.Type = class Rex_Firebase_CounterType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
