"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_Firebase_ItemMonitor;

	PLUGIN_CLASS.Type = class Rex_Firebase_ItemMonitorType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
