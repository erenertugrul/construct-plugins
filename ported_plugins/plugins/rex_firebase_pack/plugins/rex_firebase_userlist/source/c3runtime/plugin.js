"use strict";
    var clean_table = function (o)
	{
        var k;
        for (k in o)
            delete o[k];
	};	
{
	C3.Plugins.Rex_Firebase_Userlist = class Rex_Firebase_UserlistPlugin extends C3.SDKPluginBase
	{
		constructor(opts)
		{
			super(opts);
		}
		
		Release()
		{
			super.Release();
		}
	};
}