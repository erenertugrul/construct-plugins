"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_SLGMovement;

	PLUGIN_CLASS.Type = class Rex_SLGMovementType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
