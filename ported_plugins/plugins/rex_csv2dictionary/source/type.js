"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_CSV2Dictionary;

	PLUGIN_CLASS.Type = class Rex_CSV2DictionaryType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
