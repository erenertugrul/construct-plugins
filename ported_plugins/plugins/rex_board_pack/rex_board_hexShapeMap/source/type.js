"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_hexShapeMap;

	PLUGIN_CLASS.Type = class Rex_hexShapeMapType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
