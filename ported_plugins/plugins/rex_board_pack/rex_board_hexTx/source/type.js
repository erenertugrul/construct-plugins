"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_SLGHexTx;

	PLUGIN_CLASS.Type = class Rex_SLGHexTxType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
