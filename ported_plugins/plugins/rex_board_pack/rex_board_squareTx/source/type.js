"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_SLGSquareTx;

	PLUGIN_CLASS.Type = class Rex_SLGSquareTxType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
