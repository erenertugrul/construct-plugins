"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_ProjectionTx;

	PLUGIN_CLASS.Type = class Rex_ProjectionTxType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
