"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_SLGCubeTx;

	PLUGIN_CLASS.Type = class Rex_SLGCubeTxType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
