"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.Rex_TimeAwayL;

	PLUGIN_CLASS.Type = class Rex_TimeAwayLType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
