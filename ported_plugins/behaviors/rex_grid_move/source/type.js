"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_GridMove;

	BEHAVIOR_CLASS.Type = class Rex_GridMoveType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
