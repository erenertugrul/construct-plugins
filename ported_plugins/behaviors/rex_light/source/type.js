"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_light;

	BEHAVIOR_CLASS.Type = class Rex_lightType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
