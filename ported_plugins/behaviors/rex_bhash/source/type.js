"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_bHash;

	BEHAVIOR_CLASS.Type = class Rex_bHashType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
