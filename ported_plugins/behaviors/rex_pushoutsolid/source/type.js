"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_pushOutSolid;

	BEHAVIOR_CLASS.Type = class Rex_pushOutSolidType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
