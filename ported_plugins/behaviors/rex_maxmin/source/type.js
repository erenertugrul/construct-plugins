"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_maxmin;

	BEHAVIOR_CLASS.Type = class Rex_maxminType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
