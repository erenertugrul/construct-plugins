"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.rex_Anchor2;

	BEHAVIOR_CLASS.Type = class rex_Anchor2Type extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
