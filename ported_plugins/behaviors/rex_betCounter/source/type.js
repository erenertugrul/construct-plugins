"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_betCounter;

	BEHAVIOR_CLASS.Type = class Rex_betCounterType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
