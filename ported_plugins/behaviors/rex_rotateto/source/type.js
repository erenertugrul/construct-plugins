"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_RotateTo;

	BEHAVIOR_CLASS.Type = class Rex_RotateToType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
