"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.rex_anglelock;

	BEHAVIOR_CLASS.Type = class rex_anglelockType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
