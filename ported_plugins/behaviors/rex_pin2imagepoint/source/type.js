"use strict";

{
	const BEHAVIOR_CLASS = SDK.Behaviors.Rex_pin2imgpt;

	BEHAVIOR_CLASS.Type = class Rex_pin2imgptType extends SDK.IBehaviorTypeBase
	{
		constructor(sdkPlugin, iBehaviorType)
		{
			super(sdkPlugin, iBehaviorType);
		}
	};
}
