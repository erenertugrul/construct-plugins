"use strict";

{
	const PLUGIN_CLASS = SDK.Plugins.eren_firebase_remote;

	PLUGIN_CLASS.Type = class eren_firebase_remoteType extends SDK.ITypeBase
	{
		constructor(sdkPlugin, iObjectType)
		{
			super(sdkPlugin, iObjectType);
		}
	};
}
